import Section from "./Section";
import { IoLocationOutline } from "react-icons/io5";

export default function Address() {
  return (
    <Section label={"Manage Addresses"} icon={<IoLocationOutline size={18} />}>
      <div>Hello</div>
    </Section>
  );
}
